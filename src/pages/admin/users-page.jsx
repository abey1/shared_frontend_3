import AdminSectionShell from "./AdminSectionShell.jsx";

export default function AdminUsersPage() {
  return (
    <AdminSectionShell
      title="Users"
      subtitle="Cross-tenant user directory and moderation. Connect to `/users/me` patterns or admin APIs as you add them."
    />
  );
}
